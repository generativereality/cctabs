{
  "name": "@generativereality/cctabs-docs",
  "private": true,
  "scripts": {
    "dev": "vitepress dev --port 7417",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "deploy": "npm run build && wrangler pages deploy .vitepress/dist --project-name cctabs",
    "generate-og": "node scripts/generate-og.mjs"
  },
  "devDependencies": {
    "playwright": "^1.50.0",
    "vitepress": "^1.6.3",
    "wrangler": "^3.114.0"
  },
  "type": "module"
}
